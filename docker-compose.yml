services:
  stotracker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4545:4545"
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4545/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s